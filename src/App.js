import Button from './components/Button/Button';

function App() {
  return (
    <main>
      <Button onClick={() => alert('Hello, world!')}>Click me!</Button>
    </main>
  );
}

export default App;
